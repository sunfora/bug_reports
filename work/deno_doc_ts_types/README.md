# Typescript types in jsdoc are not parsed correctly (deno\_doc)

If you run `deno doc --json` with any type that has brackets inside.  
You get that it is unsupported.

[same issue opened](https://github.com/denoland/deno_doc/issues/707)
